describe('ExifInput', () => {
  it.todo('defaults to a text input');

  describe('when the exif tag has a description', () => {
    it.todo('shows the description');
  });

  describe('when the exif tag is an enum', () => {
    it.todo('is a select input');

    it.todo('has a blank option');
  });

  describe('when the exif tag is a date input', () => {
    it.todo('has a date input with seconds');

    it.todo('is valid if the date is typed or selected');
  });

  describe('when the input is invalid', () => {
    it.todo('indicates to the user, regardless of input type');
  });
});
